MONGO_URI=mongodb://localhost:27017/karunadu
# Optional Atlas fallback (only used if MONGO_URI is not set or local fails)
# MONGO_ATLAS_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority
